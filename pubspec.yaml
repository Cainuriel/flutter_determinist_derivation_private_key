name: flutter_determinist_derivation_private_key
description: "A new Flutter project."

# Prevent accidental publishing to pub.dev.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  bip32: ^2.0.0
  bip39: ^1.0.6
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  pointycastle: ^3.8.0
  web3dart: ^2.7.3
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Enable generation of localized Strings from arb files.
  generate: true

  assets:
    # Add assets from the images directory to the application.
    - assets/images/
